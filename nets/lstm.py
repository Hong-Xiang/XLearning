
from ..model.layers import Input
from .base import Net
from ..utils.general import with_config

class GenLSTM1D(self):
    def __init__(self,
                 settings=None,
                 **kwargs):
        self._models_names = ['GenLSTM1D']
        self._is_train_step = [True]
    
    def _define_model(self):
        input_ = In
    
